<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wild meassage - feed</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
   {{> nav}}

<main class="container">
  <h1>Comments</h1>
  <!--if there is a comment then set up the comment boxes -->
  {{#if comments.length}}
    <ul class="comments-list">
      <!--for each comment print out the selected information-->
      {{#each comments}}
        <li class="comment">
          <div class="comment-meta">
            <strong class = "{{profile_color}}">{{display_name}}</strong> — {{created_at}}
          </div>
          <div class="comment-body">{{content}}</div>
        </li>
      {{/each}}
    </ul>
    <!--no comment -->
  {{else}}
    <p>No comments yet. Be the first to post.</p>
  {{/if}}
  <!--if logged in then allow to comment-->
  {{#if isLoggedIn}}
    <form class="from" method="POST" action="/comments/comment">
      <textarea name="text" required></textarea>
      <br>
      <button class="btn" type="submit">Add new comment</button>
    </form>
  <!--else redirceted to log in-->
  {{else}}
    <p>You must <a href="/api/auth/login">log in</a> to post a comment.</p>
  {{/if}}
</main>

{{> footer}}

</body>
</html>

