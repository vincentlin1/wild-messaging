<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wild meassage - feed</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
   {{> nav}}

<main class="container">
  <h1>Comments</h1>
  <!--if there is a comment then set up the comment boxes -->
  {{#if comments.length}}
    <ul class="comments-list">
      <!--for each comment print out the selected information-->
      {{#each comments}}
        <li class="comment">
          <div class="comment-meta">
            <strong>{{this.author}}</strong> — {{this.timestamp}}
          </div>
          <div class="comment-body">{{this.text}}</div>
        </li>
      {{/each}}
    </ul>
  <!--no comment -->
  {{else}}
    <p>No comments yet. go to new comment to be the first to post</p>
  {{/if}}
   <!--if logged in you can see a button that take you to new comment else it'll tell you to log in first-->
  {{#if user}}
    <a class="btn" href="/new_comment">Add new comment</a>
  {{else}}
    <p>you must <a href="/login">log in</a> to post a comment.</p>
  {{/if}}
</main>

{{> footer}}

</body>
</html>

